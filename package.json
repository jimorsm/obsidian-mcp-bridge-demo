{
  "name": "obsidian-mcp-excalidraw-bridge-demo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild main.ts --bundle --external:obsidian --format=cjs --outfile=main.js",
    "dev": "esbuild main.ts --bundle --external:obsidian --format=cjs --outfile=main.js --watch"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "esbuild": "^0.20.2",
    "obsidian": "^1.5.0",
    "typescript": "^5.4.5"
  }
}
